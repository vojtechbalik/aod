"
when my value changes, events have to be sent so that view can be updated.. or does it? how will it work with BlLayout
"
Class {
	#name : #AodCassowaryAnchorImpl,
	#superclass : #ClVariable,
	#category : #'Aod-Solver-Cassowary'
}
