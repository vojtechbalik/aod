"
type that does not represent any data or operation, but serves as a way of injecting custom code into automatic type-checking
"
Class {
	#name : #AodValidatorType,
	#superclass : #AodType,
	#category : #'Aod-Language-Core'
}
